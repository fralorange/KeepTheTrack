/**
 * Defines the structure of a "next video" object for the popup.
 */
export interface PopupVideoStructure {
	videoHref: string | null;
	videoSrc: string | null;
	videoTitle: string | null;
	videoAuthor: string | null;
}
